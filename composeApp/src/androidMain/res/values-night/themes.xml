<resources>
    <!-- Splash theme — used in AndroidManifest -->
    <style name="Theme.MyContactApp" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/white</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
        <item name="windowSplashScreenAnimationDuration">500</item>
        <item name="postSplashScreenTheme">@style/Theme.MyContactApp.Main</item>
    </style>

    <!-- Main app theme — applied after splash dismisses. Uses a framework theme so no Material
         Components dependency is needed; the actual UI colors come from Compose MaterialTheme. -->
    <style name="Theme.MyContactApp.Main" parent="android:Theme.Material.NoActionBar">
    </style>

</resources>